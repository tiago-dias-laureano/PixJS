export type PixJSProps = {
  name: string;
  key: string;
  amount: number;
  city: string;
  id: string;
};

export type PixJSPropsImage = {
  name: string;
  key: string;
  amount: number;
  city: string;
  id: string;
  path?: string;
};
